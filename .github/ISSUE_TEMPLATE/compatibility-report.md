---
name: Compatibility report
about: Tell us whether the patch installed, refused, or misbehaved on your setup
labels: compatibility
---

**What happened** (installed fine / refused cleanly / something unexpected):

**Claude Code version** (`claude --version`):

**How Claude Code was installed** (paste `npm ls -g @anthropic-ai/claude-code`; empty output means a native, Homebrew or bun install, which is not supported):

**OS** (macOS version / Linux distro / WSL2):

**claude-queue state** (`claude-queue status`, or "not installed yet"):

**The exact message printed**, if it refused or failed:

Dry-run reports are welcome too, even when everything worked. Refusals on new
Claude Code releases are the most useful reports of all: they tell us the
patterns need updating before anyone gets hurt.
